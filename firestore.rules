rules_version='2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Development/Emulator rules - allow all access
    // TODO: Update these rules for production with proper authentication checks
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
