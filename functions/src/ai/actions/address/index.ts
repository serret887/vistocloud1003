/**
 * Server-side address resolution using Google Places API
 */

export { resolveAddress } from './resolveAddress';
export { resolveAddressesInActions } from './resolveAddressesInActions';
export type { AddressData } from './types';

